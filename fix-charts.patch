diff --git a/index.html b/index.html
index fce2baa..409213c 100644
--- a/index.html
+++ b/index.html
@@ -11,9 +11,7 @@
   <meta property="og:type" content="website">
   <title>Bibliometric Research Dashboard</title>
 
-  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"
-    integrity="sha512-CQBSSt0Bm2MlDJMYbBFNUEoMBCpgYJ6Clz0HIEP4VIBfPz5WFMiyYAMBMZB3JMFhjGTaIcSQrYbUvYJo5SDNQ=="
-    crossorigin="anonymous" referrerpolicy="no-referrer"></script>
+  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>
 
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
@@ -241,6 +239,7 @@
     <section id="tab-scholar" class="tab-panel" role="tabpanel" aria-labelledby="btn-scholar">
       <h2 class="card__title">Google Scholar Result Counts</h2>
       <p class="card__subtitle">Click the link to open the exact Google Scholar search. Enter the "About X results" number shown at the top.</p>
+      <div id="scope-badge-scholar" style="margin-bottom:12px;"></div>
       <div id="scholar-tables"></div>
     </section>
 
@@ -248,6 +247,7 @@
     <section id="tab-journals" class="tab-panel" role="tabpanel" aria-labelledby="btn-journals">
       <h2 class="card__title">Journal-Specific Counts</h2>
       <p class="card__subtitle">For each term/journal/year combination, click the link to open Google Scholar filtered by source. Enter the result count.</p>
+      <div id="scope-badge-journals" style="margin-bottom:12px;"></div>
       <div id="journal-tables"></div>
     </section>
 
@@ -277,6 +277,30 @@
                 style="margin-top:6px;padding:8px;border:1px solid var(--color-border-dark);border-radius:var(--radius-sm);font-family:var(--font-sans);font-size:13px;width:100px;">
             </label>
           </div>
+          <div style="margin-bottom:16px;">
+            <strong>Search Scope</strong>
+            <p style="font-size:12px;color:var(--color-text-muted);margin:4px 0 8px;">Controls where Google Scholar looks for your search terms. Changing this rebuilds all search links.</p>
+            <div style="display:flex;flex-direction:column;gap:6px;">
+              <label style="display:flex;align-items:flex-start;gap:8px;cursor:pointer;">
+                <input type="radio" name="cfg-scope" value="any" checked
+                  style="margin-top:3px;accent-color:var(--color-accent);">
+                <span><strong>Anywhere in article</strong> <span style="font-size:11px;color:var(--color-text-muted);">(default)</span><br>
+                <span style="font-size:12px;color:var(--color-text-muted);">Searches title, abstract, full text, and keywords</span></span>
+              </label>
+              <label style="display:flex;align-items:flex-start;gap:8px;cursor:pointer;">
+                <input type="radio" name="cfg-scope" value="title"
+                  style="margin-top:3px;accent-color:var(--color-accent);">
+                <span><strong>Title only</strong><br>
+                <span style="font-size:12px;color:var(--color-text-muted);">Only counts articles with the term in the title</span></span>
+              </label>
+              <label style="display:flex;align-items:flex-start;gap:8px;cursor:pointer;">
+                <input type="radio" name="cfg-scope" value="keywords"
+                  style="margin-top:3px;accent-color:var(--color-accent);">
+                <span><strong>Keywords</strong> <span style="font-size:11px;color:var(--color-text-muted);">(approximation)</span><br>
+                <span style="font-size:12px;color:var(--color-text-muted);">Uses <code>intitle:</code> + exact phrase to approximate keyword-level filtering. Note: Google Scholar has no dedicated keyword field — for precise keyword searches, use Scopus or Web of Science.</span></span>
+              </label>
+            </div>
+          </div>
           <div class="btn-group">
             <button class="btn btn--primary" onclick="applySettings()">Apply &amp; Rebuild</button>
             <button class="btn btn--danger" onclick="resetAllData()">Reset All Data</button>
@@ -308,7 +332,20 @@
           Example: <code>"your term" source:"Your Journal Name"</code>.
           The links in this dashboard are pre-built for you.</p>
 
-          <p><strong>3. Alternative tools for more rigorous results:</strong><br>
+          <p><strong>3. Search scope options:</strong><br>
+          You can change the search scope in <strong>Settings</strong> to control where Google Scholar
+          looks for your terms:<br>
+          — <strong>Anywhere in article</strong> (default): Searches title, abstract, full text, and keywords.
+          This gives the broadest count.<br>
+          — <strong>Title only</strong>: Only counts articles with the term in the title. Uses Google Scholar's
+          <code>as_occt=title</code> parameter. Yields much smaller but more focused counts.<br>
+          — <strong>Keywords (approx.)</strong>: Uses <code>allintitle:</code> as an approximation.
+          Google Scholar has no dedicated keyword field — for precise keyword-only searches, use
+          Scopus, Web of Science, or ERIC instead.<br>
+          — <em>Tip:</em> Collect data in one scope, export it, then switch scope and collect again
+          to compare across scopes.</p>
+
+          <p><strong>4. Alternative tools for more rigorous results:</strong><br>
           — <strong>Scopus</strong> or <strong>Web of Science</strong>: Allow precise filtering by
           journal, year, and keyword. They also export as CSV.<br>
           — <strong>ERIC</strong> (<a href="https://eric.ed.gov" target="_blank" rel="noopener noreferrer">eric.ed.gov</a>):
@@ -318,14 +355,14 @@
           — <strong>Dimensions</strong> (<a href="https://app.dimensions.ai" target="_blank" rel="noopener noreferrer">dimensions.ai</a>):
           Free tier available.</p>
 
-          <p><strong>4. Important caveats:</strong><br>
+          <p><strong>5. Important caveats:</strong><br>
           — Google Scholar counts are approximate and can fluctuate.<br>
           — The <code>source:</code> filter in Google Scholar is not perfectly reliable.<br>
           — For publication-quality data, Scopus or Web of Science are preferred.<br>
           — Record the date you collected data, as counts change over time.<br>
           — Partial-year data will naturally show lower numbers.</p>
 
-          <p><strong>5. Interpreting your charts:</strong><br>
+          <p><strong>6. Interpreting your charts:</strong><br>
           — Line charts show trends and turning points.<br>
           — Bar charts make year-to-year comparison easier.<br>
           — Stacked area charts show how journals contribute to the total.<br>
@@ -366,6 +403,7 @@
     journals: ['Journal Name 1', 'Journal Name 2', 'Journal Name 3'],
     yearStart: 2011,
     yearEnd: 2026,
+    searchScope: 'any',
   };
 
   var scholarData = {};
@@ -381,10 +419,29 @@
   }
 
   function scholarURL(term, year) {
+    var scope = CONFIG.searchScope || 'any';
+    if (scope === 'title') {
+      return 'https://scholar.google.com/scholar?as_q=&as_epq=' + encodeURIComponent(term) +
+        '&as_occt=title&as_ylo=' + year + '&as_yhi=' + year + '&as_vis=1';
+    }
+    if (scope === 'keywords') {
+      return 'https://scholar.google.com/scholar?q=allintitle:%22' + encodeURIComponent(term) +
+        '%22&as_ylo=' + year + '&as_yhi=' + year + '&as_vis=1';
+    }
     return 'https://scholar.google.com/scholar?q=%22' + encodeURIComponent(term) + '%22&as_ylo=' + year + '&as_yhi=' + year;
   }
 
   function journalURL(term, year, journal) {
+    var scope = CONFIG.searchScope || 'any';
+    if (scope === 'title') {
+      return 'https://scholar.google.com/scholar?as_q=&as_epq=' + encodeURIComponent(term) +
+        '&as_occt=title&as_publication=' + encodeURIComponent(journal) +
+        '&as_ylo=' + year + '&as_yhi=' + year + '&as_vis=1';
+    }
+    if (scope === 'keywords') {
+      return 'https://scholar.google.com/scholar?q=allintitle:%22' + encodeURIComponent(term) +
+        '%22+source:%22' + encodeURIComponent(journal) + '%22&as_ylo=' + year + '&as_yhi=' + year + '&as_vis=1';
+    }
     return 'https://scholar.google.com/scholar?q=%22' + encodeURIComponent(term) + '%22+source:%22' + encodeURIComponent(journal) + '%22&as_ylo=' + year + '&as_yhi=' + year;
   }
 
@@ -413,6 +470,7 @@
         CONFIG.journals = state.config.journals || CONFIG.journals;
         CONFIG.yearStart = state.config.yearStart || CONFIG.yearStart;
         CONFIG.yearEnd = state.config.yearEnd || CONFIG.yearEnd;
+        CONFIG.searchScope = state.config.searchScope || CONFIG.searchScope;
       }
       if (state.scholarData) scholarData = state.scholarData;
       if (state.journalData) journalData = state.journalData;
@@ -572,6 +630,11 @@
     container.innerHTML = html;
   }
 
+  // safe data access — || null treats 0 as null, so we use explicit null check
+  function getVal(obj, key) { return obj && obj[key] != null ? obj[key] : null; }
+  function getScholar(t, y) { return scholarData[t] ? getVal(scholarData[t], y) : null; }
+  function getJournal(t, j, y) { return journalData[t] && journalData[t][j] ? getVal(journalData[t][j], y) : null; }
+
   // charts
   function chartOptions() {
     return {
@@ -586,6 +649,17 @@
   }
 
   function initCharts() {
+    if (typeof Chart === 'undefined') {
+      console.error('Chart.js library not loaded');
+      var containers = [document.getElementById('lineChart'), document.getElementById('barChart')];
+      containers.forEach(function(c) {
+        if (c && c.parentNode) {
+          c.parentNode.innerHTML = '<div style="display:flex;align-items:center;justify-content:center;height:100%;font-family:var(--font-sans);color:var(--color-accent);font-size:14px;text-align:center;padding:20px;">' +
+            '⚠ Chart.js failed to load. Please check your internet connection and reload the page.</div>';
+        }
+      });
+      return;
+    }
     var years = getYears();
     if (lineChart) lineChart.destroy();
     if (barChart) barChart.destroy();
@@ -598,7 +672,7 @@
         labels: years,
         datasets: CONFIG.terms.map(function(t, i) {
           return {
-            label: t, data: years.map(function(y) { return scholarData[t] ? scholarData[t][y] || null : null; }),
+            label: t, data: years.map(function(y) { return getScholar(t, y); }),
             borderColor: termColor(i), backgroundColor: termColorAlpha(i),
             tension: 0.3, pointRadius: 3, borderWidth: 2.5, spanGaps: true,
           };
@@ -613,7 +687,7 @@
         labels: years,
         datasets: CONFIG.terms.map(function(t, i) {
           return {
-            label: t, data: years.map(function(y) { return scholarData[t] ? scholarData[t][y] || null : null; }),
+            label: t, data: years.map(function(y) { return getScholar(t, y); }),
             backgroundColor: termColor(i) + 'cc', borderColor: termColor(i), borderWidth: 1, borderRadius: 3,
           };
         }),
@@ -631,7 +705,7 @@
           datasets: CONFIG.journals.map(function(j, ji) {
             return {
               label: j,
-              data: years.map(function(y) { return journalData[t] && journalData[t][j] ? journalData[t][j][y] || null : null; }),
+              data: years.map(function(y) { return getJournal(t, j, y); }),
               borderColor: PALETTE[ji % PALETTE.length], backgroundColor: PALETTE_ALPHA[ji % PALETTE_ALPHA.length],
               fill: true, tension: 0.3, pointRadius: 2, borderWidth: 2, spanGaps: true,
             };
@@ -645,7 +719,7 @@
   function refreshCharts() {
     var years = getYears();
     CONFIG.terms.forEach(function(t, ti) {
-      var sData = years.map(function(y) { return scholarData[t] ? scholarData[t][y] || null : null; });
+      var sData = years.map(function(y) { return getScholar(t, y); });
       if (lineChart && lineChart.data.datasets[ti]) lineChart.data.datasets[ti].data = sData;
       if (barChart && barChart.data.datasets[ti]) barChart.data.datasets[ti].data = sData;
     });
@@ -657,7 +731,7 @@
       CONFIG.journals.forEach(function(j, ji) {
         if (journalCharts[ti].data.datasets[ji]) {
           journalCharts[ti].data.datasets[ji].data = years.map(function(y) {
-            return journalData[t] && journalData[t][j] ? journalData[t][j][y] || null : null;
+            return getJournal(t, j, y);
           });
         }
       });
@@ -671,6 +745,8 @@
     document.getElementById('cfg-journals').value = CONFIG.journals.join('\n');
     document.getElementById('cfg-year-start').value = CONFIG.yearStart;
     document.getElementById('cfg-year-end').value = CONFIG.yearEnd;
+    var radios = document.querySelectorAll('input[name="cfg-scope"]');
+    radios.forEach(function(r) { r.checked = (r.value === CONFIG.searchScope); });
   }
 
   function applySettings() {
@@ -687,6 +763,8 @@
     CONFIG.journals = journals;
     CONFIG.yearStart = yearStart;
     CONFIG.yearEnd = yearEnd;
+    var scopeRadio = document.querySelector('input[name="cfg-scope"]:checked');
+    CONFIG.searchScope = scopeRadio ? scopeRadio.value : 'any';
 
     initializeData();
     rebuildAll();
@@ -703,6 +781,7 @@
     CONFIG.journals = ['Journal Name 1', 'Journal Name 2', 'Journal Name 3'];
     CONFIG.yearStart = 2011;
     CONFIG.yearEnd = 2026;
+    CONFIG.searchScope = 'any';
     initializeData();
     rebuildAll();
     showToast('All data has been reset');
@@ -711,18 +790,19 @@
   // export / import
   function exportCSV() {
     var years = getYears();
-    var csv = 'Source,Term,Year,Count\n';
+    var scopeLabel = (SCOPE_LABELS[CONFIG.searchScope] || SCOPE_LABELS.any).label;
+    var csv = 'Source,Term,Year,Count,SearchScope\n';
     CONFIG.terms.forEach(function(t) {
       years.forEach(function(y) {
         var v = scholarData[t] && scholarData[t][y] != null ? scholarData[t][y] : '';
-        csv += 'Google Scholar,"' + t + '",' + y + ',' + v + '\n';
+        csv += 'Google Scholar,"' + t + '",' + y + ',' + v + ',"' + scopeLabel + '"\n';
       });
     });
     CONFIG.terms.forEach(function(t) {
       CONFIG.journals.forEach(function(j) {
         years.forEach(function(y) {
           var v = journalData[t] && journalData[t][j] && journalData[t][j][y] != null ? journalData[t][j][y] : '';
-          csv += '"' + j + '","' + t + '",' + y + ',' + v + '\n';
+          csv += '"' + j + '","' + t + '",' + y + ',' + v + ',"' + scopeLabel + '"\n';
         });
       });
     });
@@ -748,6 +828,7 @@
           CONFIG.journals = data.config.journals || CONFIG.journals;
           CONFIG.yearStart = data.config.yearStart || CONFIG.yearStart;
           CONFIG.yearEnd = data.config.yearEnd || CONFIG.yearEnd;
+          CONFIG.searchScope = data.config.searchScope || CONFIG.searchScope;
         }
         if (data.scholarData) scholarData = data.scholarData;
         if (data.journalData) journalData = data.journalData;
@@ -774,6 +855,29 @@
     URL.revokeObjectURL(a.href);
   }
 
+  // scope badge
+  var SCOPE_LABELS = {
+    any: { label: 'Anywhere in article', icon: '📄' },
+    title: { label: 'Title only', icon: '🔤' },
+    keywords: { label: 'Keywords (approx.)', icon: '🏷️' },
+  };
+
+  function renderScopeBadges() {
+    var scope = CONFIG.searchScope || 'any';
+    var info = SCOPE_LABELS[scope] || SCOPE_LABELS.any;
+    var badgeHtml = '<div style="display:inline-flex;align-items:center;gap:6px;padding:6px 14px;' +
+      'background:var(--color-info-bg);border:1px solid var(--color-info-border);border-radius:20px;' +
+      'font-family:var(--font-sans);font-size:12px;">' +
+      '<span>' + info.icon + '</span>' +
+      '<span>Search scope: <strong>' + info.label + '</strong></span>' +
+      '<a href="#" onclick="switchTab(\'settings\');return false;" style="color:var(--color-accent);font-size:11px;margin-left:4px;">change</a>' +
+      '</div>';
+    var el1 = document.getElementById('scope-badge-scholar');
+    var el2 = document.getElementById('scope-badge-journals');
+    if (el1) el1.innerHTML = badgeHtml;
+    if (el2) el2.innerHTML = badgeHtml;
+  }
+
   // full rebuild
   function rebuildAll() {
     buildScholarTables();
@@ -782,6 +886,7 @@
     buildJournalChartContainers();
     initCharts();
     populateSettings();
+    renderScopeBadges();
   }
 
   // kick it off
